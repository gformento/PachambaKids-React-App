export const Banner = ({ imgBanner }) => {
  return (
    <div class="card text-white container-fluid">
      <img
        src={imgBanner}
        class="logo"
        alt="..."
        width="100%"
        height="500px"
      ></img>

      <br></br>
    </div>
  );
};
